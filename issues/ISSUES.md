# Issues — Menú Pro

## Abiertos

| ID | Título | Módulo | Reproducible | Prioridad |
|----|--------|--------|--------------|-----------|

_(vacío)_




## En análisis

_(vacío)_

## Fix pendiente (diagnosticado, sin implementar)

| ID | Título | Módulo | Prioridad |
|----|--------|--------|-----------|
| [ISS-059](ISS-059-revertir-pedido-cancelado.md) | Sin forma de revertir un pedido cancelado por error | `orders.js`/`reservations.js`/`pedidos.js` | 🔴 Alta |
| [ISS-060](ISS-060-acceso-pensionista-menu.md) | Pensionistas sin un camino claro para llegar a `pensionista.html` | `menu.html`/`pensionista.html` | 🟡 Media |
| [ISS-077](ISS-077-novedades-js-fuera-de-assets.md) | Módulos JS fuera del precache/versionado (`novedades.js`, `charts-theme-admin.js`) | `sw.js`/`admin/dashboard.html` | 🟢 Baja |

## Resueltos

| ID | Título | Fecha resolución | Solución |
|----|--------|-----------------|---------|
| [ISS-076](ISS-076-que-hay-de-nuevo.md) | Sin forma de avisar cambios a la dueña salvo explicarlos en persona/teléfono | 2026-08-25 | Modal "🎉 Qué hay de nuevo" (una vez al abrir, compara `localStorage`) + badge "🆕" por control nuevo (14 días o hasta tocarlo). Se descartó un tour interactivo por costo de mantenimiento. Solo owner/admin. |
| [ISS-075](ISS-075-agregar-manual-simplificado.md) | "Agregar manual" no se le pegaba a la dueña — modal-sobre-modal con fotos que no necesitaba | 2026-08-25 | Lista plana de nombres inline (sin PlatoPicker ni fotos) para elegir plato por sección; Mesa/Nombre marcados "(opcional)". Carta ya cumplía, sin cambios. Sin backend. |
| [ISS-074](ISS-074-mesa-grande-orden-chico.md) | Mesa aparecía chica/gris y #orden grande — al revés de lo que necesita cocina | 2026-08-25 | Invertida la jerarquía en Cola, Cocina, Órdenes y Reservas: Mesa en negrita, #orden chico (para llevar/delivery sin mesa quedan como antes). |
| [ISS-073](ISS-073-anti-parpadeo-cocina-ordenes-reservas.md) | Cocina/Órdenes/Reservas parpadeaban en cada refresco, mismo bug que ISS-067 nunca portado ahí | 2026-08-25 | Helper compartido `pintarSiCambio()` en `utils.js`, aplicado a los 3 paneles que no lo tenían. La Cola del día no se tocó, ya funcionaba bien. |
| [ISS-072](ISS-072-cobro-en-un-clic.md) | 2 clics para cobrar Yape/Plin vs. 1 con efectivo — confuso y el paso extra no cumplía su función real | 2026-08-25 | Un solo botón hace las 2 llamadas que antes requerían 2 taps, en los 4 lugares con el mismo patrón (reservas.js, ordenes.js, pedidos.js x2). Sin cambios de backend, el candado del servidor lo sigue satisfaciendo. Decisión de la dueña tras explicar el porqué del paso extra. |
| [ISS-071](ISS-071-reservas-en-medio-bottom-nav.md) | "Reservas" en el medio del bottom-nav causaba toques por error queriendo entrar a "Cola" | 2026-08-25 | Botón `#bn-reservas` oculto del bottom-nav (`.hidden`) — redundante desde ISS-067, la Cola del día ya muestra reservas con las mismas acciones. Panel sigue accesible desde el menú lateral. |
| [ISS-070](ISS-070-compatibilidad-platos-opcion-a.md) | Relación Exige/No permite sección escondida detrás de "⋯" con nombres técnicos | 2026-08-25 | Opción A del mockup elegida: control de 3 estados siempre visible por plato (🔓 Puede llevar / ✅ Necesita / 🚫 No lleva), uno por cada sección opcional relacionada, con tooltip ⓘ. Reusa los 2 endpoints PATCH existentes, sin cambios de backend. Reemplaza los 2 modales viejos detrás de "⋯". |
| [ISS-069](ISS-069-deseleccion-y-tap-en-foto.md) | No se podía deseleccionar un plato del menú (radio nativo) + tocar la foto no elegía el plato | 2026-08-25 | El radio ahora recibe `checked` según la selección real (se perdía visualmente en cada `refresh()`, bug raíz compartido); tocar de nuevo la misma fila la deselecciona vía nueva `deselectMenuPlato()`; se quitó el zoom de foto en platos elegibles — tocarla selecciona, igual que el resto de la fila. Reportado por el usuario, sin visita de piloto de por medio. |
| [ISS-068](ISS-068-stock-rapido-desde-cola.md) | Marcar "Agotado" era demasiado lento en plena hora pico | 2026-08-24 | Nuevo botón "📦 Stock" en el header de Cola del día — lista plana de todos los platos de hoy (sin acordeón de secciones) con toggle de 1 tap "⛔ Agotado". Reusa el endpoint existente, sin cambios de backend. Visita en persona, día 11 del piloto. |
| [ISS-067](ISS-067-cola-refresco-y-reservas-sin-hora.md) | Cola del día: reservas sin hora enterradas + parpadeo en cada refresco | 2026-08-24 | Reservas sin `hora_llegada` tenían urgencia fija en 0 y quedaban enterradas debajo de todo lo activo — ahora se ordenan por antigüedad, igual que las órdenes. `renderZona()` ganó una "firma" anti-parpadeo (no repinta si los datos no cambiaron) y `renderColaDesdeCache()` preserva el `scrollTop` de `.content` al repintar. Polling bajado de 30s a 60s. Visita en persona, día 11 del piloto — confirmado en vivo por el usuario. |
| [ISS-066](ISS-066-plato-no-permite-seccion-opcional.md) | Un plato "autocontenido" podía combinarse igual con una sección opcional que no necesita | 2026-08-24 | Inverso de ISS-046: nuevo `componentes_menu_dia.no_permite_seccion_id` — un plato puede bloquear otra sección opcional (ej. "ají de gallina" no permite "Proteínas"), solo aplica si esa sección es opcional en el menú (si es obligatoria se ignora, se exige igual). `utils/validarSeccionesMenu.js` gana la 3ª regla; `menu-modal.js` deshabilita la sección bloqueada en vivo y limpia la selección si ya había una. Planteado por el usuario razonando sobre el caso de ISS-046. 458/458 jest. |
| [ISS-065](ISS-065-reserva-sin-hora-bloqueada.md) | Reservar sin hora podía bloquearse por el horario de "ahora", no el de la reserva | 2026-08-24 | `validarHorarioReserva()` caía a `validarHorarioAhora()` (fecha de HOY) cuando no había hora_llegada, en vez de simplemente permitir siempre — la hora nunca debió ser un requisito, solo ayuda informativa (no hay ningún paso automático que la use). Reportado por el usuario, día 10 del piloto: un comensal no pudo reservar. Sin hora ahora siempre permite; con hora sigue validando esa hora específica (D1). |
| [ISS-064](ISS-064-repetir-mismo-menu.md) | Pedir 2 menús idénticos exigía reabrir el picker y volver a elegir todo | 2026-08-24 | Cada unidad sigue siendo su propia fila del carrito (compatible con ISS-041), pero se agrupan visualmente con un stepper (`agruparMenusCarrito`) y un atajo "+1 mismo menú" al agregar (`mostrarAtajoRepetirMenu`). Aplica a Pedir y Reservar. Mockup aprobado antes de codear. 17/17 E2E nuevo (`test-repetir-menu.js`) + 454/454 jest sin regresiones. |
| [ISS-063](ISS-063-reservas-formulario-antes-de-carta.md) | En Reservas se pedían los datos del comensal antes de mostrar la carta | 2026-08-24 | El formulario completo (fecha/hora/nombre/teléfono) iba arriba de todo — ahora replica el patrón de "Pedir": la carta primero (`#res-menu-content`), los datos en un drawer nuevo (`#res-drawer`) que se abre al tocar la barra inferior. Mockup aprobado antes de codear. 15/15 + 24/24 E2E (`test-iss048-volver-pago.js`, `test-gate-pago.js`, actualizados al nuevo flujo) + 454/454 jest. |
| [ISS-062](ISS-062-boton-listo-zona-cocina.md) | Zona "Cocina" de la Cola del día solo permitía cancelar, no marcar "Listo" | 2026-08-24 | `btnOrden()`/`btnReserva()` no manejaban `zona === 'cocina'`. Nuevo caso que reusa `accionRapidaOrden/Reserva(id,'es_listo')` — el backend ya aceptaba la transición. Cierra el ciclo simétrico con "↩️ Regresar a cocina" (ISS-055). 454/454 jest sin regresiones. |
| [ISS-061](ISS-061-status-preparando-confuso.md) | Status "En preparación" no le dejaba claro al cliente que su pedido estaba en curso | 2026-08-24 | Cambio de copy en `STATUS_MAP` (`menu.html`): "En preparación" → "Ya estamos cocinando tu pedido". Los 4 hallazgos de este día vienen del día 9 del piloto, reportado por el usuario. 454/454 jest. |
| [ISS-058](ISS-058-historial-ordenes-sin-comprobante.md) | Historial de Órdenes no mostraba la foto del comprobante | 2026-08-21 | La query de `GET /api/orders` nunca seleccionaba `metodo_pago`/`estado_pago`/`comprobante_url`/campos de duplicado — el frontend ya sabía pintarlos (`comprobanteThumb()`), pero la condición `o.metodo_pago \|\| o.es_manual` siempre daba falso. `GET /api/reservations` no tenía el bug. Se agregaron las columnas faltantes al `SELECT`. Reportado por la dueña, día 7 del piloto: *"¿No puedo ver las fotos pasadas? porque quiero revalidar..."*. 457/457 jest. |
| [ISS-057](ISS-057-letra-ajustable-menu-cliente.md) | Tamaño de letra ajustable en la carta del cliente (`menu.html`) | 2026-08-21 | El tamaño de letra ajustable (`ISS-028`) era solo del panel del owner — `menu.html` quedó pendiente en su momento. Mismo mecanismo (`--font-scale` en `<html>`, aplicado antes del paint): 63 declaraciones de `font-size` en `menu.css` convertidas de `px` a `rem`, botón 🔤 nuevo en el header (3 niveles, `localStorage` propio). Reportado por el usuario, día 7 del piloto: un cliente no alcanzaba a leer la carta. 457/457 jest. |
| [ISS-056](ISS-056-volver-de-yape-instrucciones.md) | Cliente no sabía cómo volver a la app tras pagar por Yape/Plin | 2026-08-21 | Distinto de `ISS-049` (el pedido no se perdía, era confusión de navegación entre apps). Bloque de instrucción nuevo bajo el número de Yape/Plin en `menu.html`, solo en esos 2 métodos. 100% frontend, sin backend. 457/457 jest. |
| [ISS-055](ISS-055-regresar-listos-a-cocina.md) | Sin forma de regresar un pedido de "Listo" a "Cocina" | 2026-08-21 | El backend ya aceptaba el cambio de flag hacia atrás (`orders.js`/`reservations.js` solo bloquean pagado/cancelado) — faltaba el botón. Nuevo "↩️ Regresar a cocina" en la zona Listos de la Cola del día (`pedidos.js`), por ítem individual. Reportado por la cocinera, día 7 del piloto. 457/457 jest. |
| [ISS-054](ISS-054-stock-agregar-manual.md) | "Agregar manual" no respetaba el stock por plato | 2026-08-20 | El picker de "Agregar manual" (ISS-053) filtraba solo por `agotado`, nunca por `stock_restante` — un plato sin porciones seguía apareciendo elegible si nadie lo había marcado "Agotado" a mano, a diferencia de `menu.html` que ya filtraba ambas. Nueva `platoDisponibleManual(p)` compartida en `pedidos.js` con el mismo criterio que el lado del cliente. De paso se confirmó que el descuento de stock ocurre correctamente al confirmar (no antes ni después) en los 4 puntos de entrada (orden/reserva del cliente, orden del mozo, reserva del owner). 30/30 E2E (antes 28) + 457/457 jest sin regresiones. |
| [ISS-053](ISS-053-agregar-manual-con-fotos.md) | "Agregar manual" con fotos + soporte de carta | 2026-08-19 | El plato de cada sección se elegía con un `<select>` de texto plano, y solo tenía menú del día (nada de carta). Se reusó `PlatoPicker` (grid de fotos, ya existía y ya estaba en producción para armar menús en Configuración) — chip nuevo reemplaza al `<select>`, abre el mismo picker. Se sumó carta con el mismo patrón card+stepper del menú — `POST /api/orders` ya aceptaba `carta_items`, cero cambios de backend. Diseñado sobre mockup antes de codear. 28/28 E2E (`test-agregar-manual.js`, actualizado) + 457/457 jest + `test-modalidad-mixta` 19/19 sin regresiones. |
| [ISS-052](ISS-052-pensionista-sin-cambiar-password.md) | El pensionista no podía cambiar su propia contraseña | 2026-08-19 | `pensionista.html` no tenía el modal "Cambiar contraseña" que ya existe en `owner.html`. El backend no necesitó cambios — `PATCH /api/auth/me/password` ya funciona para cualquier usuario autenticado, sin filtrar por rol. Botón + modal nuevos, mismo patrón que `owner.html`. 12/12 E2E nuevo (`test-pensionista-password.js`, confirma que la contraseña vieja deja de funcionar y la nueva sí) + 457/457 jest + `test-pensionista-cliente` 29/29 sin regresiones. |
| [ISS-051](ISS-051-comprobante-duplicado.md) | Detección de comprobante Yape/Plin reutilizado | 2026-08-19 | Pregunta de la dueña, día 4 del piloto. Se avisa al owner (que ya revisa cada comprobante antes de confirmar el pago), no se bloquea al comensal. `utils/comprobanteDuplicado.js` nuevo: hashea el archivo al subirlo y busca coincidencias en `ordenes`/`reservas` del mismo restaurante, excluyendo el propio registro. Columnas `comprobante_hash`/`comprobante_repetido_de`/`comprobante_repetido_tipo` nuevas. `comprobanteThumb()` (compartida por Órdenes/Reservas/Cola del día) pinta "⚠️ Ya usado en el pedido/la reserva #N". Solo atrapa el archivo idéntico, no una recaptura. 8/8 unitarios + 7/7 E2E + 457/457 jest. |
| [ISS-050](ISS-050-numero-de-pedido-no-coincide.md) | El número de pedido que ve el comensal no coincide con el de la dueña | 2026-08-19 | El comensal veía `id_orden` (crudo, sigue de corrido — podía ser #96 con solo 22 pedidos ese día); la dueña ya veía `numero_dia` (1, 2, 3… por día) en sus vistas. `POST /api/public/orders` ahora calcula y devuelve `numero_dia` con el mismo criterio, y `menu.html` lo muestra en la confirmación. Reportado por una clienta ("mi orden me sale 96") y detectado por el usuario. 10/10 E2E + 457/457 jest. |
| [ISS-049](ISS-049-pedido-se-pierde-al-salir-a-pagar.md) | El pedido se pierde si la pestaña se recarga sola al salir a pagar | 2026-08-19 | `menu.html` no persistía nada del pedido en curso — en un celular de gama media, Chrome puede descargar la pestaña de fondo mientras el comensal paga en Yape/Plin y recargarla de cero al volver. Reportado por la dueña: *"si se les reinicia cada vez que pagan van a aburrirse de usar la app"*. Se guarda `pagoPendiente` en `localStorage` desde que elige método de pago (justo antes de salir), y se restaura al recargar — mismo total, mismo método marcado, sin duplicar la orden si ya se había creado. La foto del comprobante no se persiste (un `File` no sobrevive un `JSON.stringify`), hay que volver a adjuntarla. 12/12 E2E nuevo (`test-iss049-recuperar-pago.js`, simula la recarga real con `page.reload()`) + 449/449 jest + `test-iss048-volver-pago` 15/15 y `test-modalidad-mixta` 19/19 sin regresiones. |
| [ISS-048](ISS-048-sin-volver-desde-pago.md) | Sin forma de volver atrás desde "¿Cómo vas a pagar?" | 2026-08-19 | `#pago-screen` no tenía ningún botón de volver, a diferencia de `#repaso-screen` (que sí lo tenía, pero solo retrocedía un paso). Se confirmó que en este punto todavía no se crea ni la orden ni la reserva — es puramente un hueco de navegación. Botón `←` nuevo + `volverDePago()`: oculta la pantalla y, si es un pedido, reabre el drawer que `confirmarPedido()` había cerrado; en reserva alcanza con ocultar, porque no se cerró nada. 15/15 E2E nuevo (`test-iss048-volver-pago.js`) + 449/449 jest + `test-modalidad-mixta` 19/19 sin regresiones. |
| [ISS-047](ISS-047-modalidad-por-menu.md) | No se puede pedir un menú para llevar y otro para comer en el local | 2026-08-19 | `modalidad` vivía solo en `ordenes`/`reservas`, una por pedido: era todo o nada. Ahora va también en las líneas (cada instancia de menú comparte la de su `grupo`, ISS-041; cada plato de carta lleva la suya) y la del pedido pasa a ser el resumen derivado (`en_local` \| `para_llevar` \| `mixto` \| `delivery`), así nada de lo que la lee hoy se rompe. Nuevo `utils/modalidadPedido.js` compartido. UI: toggle «Todo aquí / Todo para llevar» arriba del carrito que marca todo de una y refleja el estado (mezclado = ninguno marcado), + chip por ítem. Cocina y Cola del día muestran «🥡 N de M para llevar» arriba y un badge por menú. **De paso arregla un cobro de más:** el envase se cobraba por el pedido entero, así que 1 menú para llevar + 1 para comer ahí cobraba 2 tappers en vez de 1. También se cerró que las órdenes nunca validaban `para_llevar_activo`. 19/19 unitarios + 19/19 E2E + 449/449 jest. |
| [ISS-046](ISS-046-plato-exige-seccion-condicional.md) | Un plato "no autocontenido" (ej. arroz con papas fritas) puede pedirse sin su sección condicional (Proteínas) | 2026-08-17 | `requerido` era una propiedad de la sección completa, no del plato — "Proteínas" opcional a propósito para que "arroz con pollo" pasara sin ella, pero eso dejaba pasar también los platos que sí la necesitaban. El backend tampoco validaba nada de esto, solo el frontend. Fix: `componentes_menu_dia.requiere_seccion_id` (genérico, no hardcodea "proteína") + `utils/validarSeccionesMenu.js` compartido por los 4 endpoints que crean pedidos/reservas, validando **por instancia de menú** (agrupa por `grupo`, ISS-041) para no dejar pasar una instancia incompleta "prestándose" la selección de otra. UI en `owner.html` para marcarlo por plato + bloqueo real en `menu.html`. 11/11 tests nuevos + 32/32 suites, 423/423 jest. |
| [ISS-045](ISS-045-slug-case-sensitive.md) | Link del menú da "Cannot GET" si llega con mayúscula | 2026-08-17 | El slug siempre se guarda en minúsculas, pero la búsqueda en `app.js` era case-sensitive (`slug = ?`). Cuando un link se abría desde otra app (WhatsApp, notas), la autocorrección del teclado solía capitalizar la primera letra (`karinamenu` → `Karinamenu`) y la ruta caía en 404, aunque tipeando la URL a mano sí funcionaba. Fix: `COLLATE NOCASE` en las dos consultas de resolución de slug (`/:slug` y `/:slug/:mesa`). Verificado con 3 variantes de mayúsculas contra el mismo restaurante + 412/412 jest (número corregido 2026-08-17: el 758 original contaba duplicado un git worktree abandonado, ver ISS-046). |
| [ISS-044](ISS-044-panel-vacio-tras-deploy.md) | Tras un deploy el panel puede aparecer vacío — parece pérdida de datos y no lo es | 2026-08-16 | Ocurrió en producción: el owner vio "ningún menú, ningún plato" y estuvo a punto de restaurar un backup sobre una base sana. Causa: los módulos JS se pedían siempre con la misma URL, así que un deploy podía dejar un `utils.js` viejo junto a un `cocina.js` nuevo → `ReferenceError` → listas vacías con los datos intactos. Fix: una sola versión de build (`utils/buildVersion.js`) que `app.js` inyecta en los HTML y en `sw.js`; cada asset se pide con `?v=BUILD`, así **todas las URLs cambian juntas** y no puede haber mezcla. De paso se resolvió **T11**: el SW pasó de precachear 7 archivos a 22 (antes no cacheaba ni un JS) con `cache:'reload'`, y los CDN pesados y las fuentes dejaron de bloquear el render. 25/25 + 11/11 en navegador + 412/412 jest. |
| [ISS-041](ISS-041-menus-multiples-sin-anidar.md) | Dos menús del día en un mismo pedido no se pueden diferenciar | 2026-08-16 | El dato se perdía **antes** de llegar al backend: `confirmarPedido()`/`confirmarReserva()` aplanaban el carrito con `flatMap` y las tablas no tenían columna de agrupación. Fix en 5 capas: columna `grupo` (migración idempotente) en `orden_menu_items`/`reserva_menu_items`; `numerarGrupos()` en `menu.html` numera por posición en el carrito; los 4 INSERT la guardan (incluido el que convierte reserva→orden, que la hereda); los SELECT de detalle devuelven `grupo` + `menu_nombre`; y una sola `renderMenuAgrupado()` en `utils.js` agrupa en las 4 vistas. Decidido con el usuario sobre mockups a 360 px: encabezado por menú (no recuadro ni número al costado), numerar siempre, nombre del menú solo si el pedido mezcla tipos, y **sin backfill** de pedidos viejos. 412/412 jest + 26/26 + 13/13 punta a punta por HTTP. |
| [ISS-040](ISS-040-monto-no-visible-en-pago.md) | Monto a pagar no visible en la pantalla de Yape/Plin | 2026-08-16 | El total ya se calculaba (`pagoPendiente.total`, incluye tapper y delivery) pero nunca se pintaba en `#pago-screen`: el comensal lo veía antes (carrito, ya cerrado) y después (repaso final, ya transferido), nunca en el momento de abrir Yape. Fix: bloque "Total a pagar" en `#pago-screen`, **sticky** para que no se pierda al bajar a subir el comprobante, y `showPagoStep()` lo pinta desde `pagoPendiente.total`. Sirve igual para efectivo (cuánto preparar). 100% frontend. SW bumpeado a `menupro-v9` (`menu.html` está en `ASSETS`). Verificado con `scripts/test-monto-pago-visible.js` (9/9 a 360×600, incluye sticky y ausencia de overflow) + 408/408 jest. |
| [ISS-042](ISS-042-para-llevar-no-viaja-cocina.md) | Etiqueta "para llevar" no llega a la vista de Cocina | 2026-08-16 | `modalidad` ya viajaba desde el backend (`utils/colaDia.js`) pero `cocina.js` no la leía. Fix: badge 🥡/🛵 en `renderCocinaTicket()` y `renderCocinaReserva()`, en línea propia (en 360px compite con el badge de estatus si va en el header). `badgeModalidad()` **se movió de `ordenes.js` a `utils.js`** con un parámetro `grande`: es el mismo widget que ya usaban Órdenes/Reservas/Cola, y desde `utils.js` no depende del orden de carga de los `<script>` de `owner.html` (cocina.js se carga antes que ordenes.js). 100% frontend, sin cambios de backend ni de esquema. Verificado con `scripts/test-badge-modalidad-cocina.js` (15/15) + 408/408 jest. |
| [ISS-039](ISS-039-pedido-enviando-colgado.md) | Pedido queda trabado en "Enviando…" desde la vista del cliente | 2026-08-13 | Reportado por el usuario: clic en "Confirmar pedido", se quedó en "Enviando…", refrescó y recién ahí funcionó. Causa: ninguno de los `fetch()` del flujo de pedido/reserva tenía timeout — en una conexión floja, el `await` puede no resolver nunca y el botón queda congelado sin feedback. Agravante: el pedido se crea en el paso 1 (JSON) *antes* de subir la foto del comprobante en el paso 2; reintentar desde cero podía duplicar el pedido en cocina. Fix: nueva `fetchConTimeout()` (15s JSON, 30s subida de foto) con mensaje claro al agotarse; `pagoPendiente.creado` guarda el id ya creado para que un reintento solo repita el PATCH de pago, nunca la creación; botón con texto por paso ("Enviando pedido…" / "Subiendo comprobante…"). 408/408 jest verde (fix 100% frontend). |
| [ISS-038](ISS-038-modal-foto-tapado-por-menumodal.md) | Modal de zoom de foto aparece tapado al elegir platos de un menú | 2026-08-13 | Reportado por el usuario: al tocar la foto de un plato dentro de la hoja de selección (`MenuModal`), el zoom no aparecía encima — recién se veía al tocar "Agregar pedido". Causa: `#photo-modal` tenía `z-index: 110`, muy por debajo de `.mm-overlay` (`z-index: 1500`), así que se abría tapado por la hoja. Subido a `z-index: 1600`. Verificado con `scripts/test-photo-modal-zindex.js` (Playwright, 6/6 verde) — simula el tap real y confirma con `elementFromPoint()` que el modal de foto queda visible arriba. |
| [ISS-035](ISS-035-scroll-no-vuelve-arriba.md) | Al cambiar de panel el scroll no vuelve arriba — el botón "← Volver" nace fuera de pantalla | 2026-08-12 | Reportado por la dueña del piloto: no encontraba la flecha de volver ("subí un poquito, ahí está"). No había ni un `scrollTo`/`scrollTop` en todo `owner.html`. El fix obvio (`window.scrollTo`) no habría servido: el scroll vive en `.content` (`overflow-y:auto`, `owner.css:322`), también en móvil. Nueva `scrollPanelArriba()` llamada desde `showPanel()` y `switchTab()`. SW bumpeado a v8 porque `owner.html` está precacheado. |
| [ISS-034](ISS-034-cierre-caja-sin-comprobante.md) | Cierre de caja: exige confirmar el pago pero no muestra el comprobante ni el botón para hacerlo | 2026-08-12 | Reportado por la dueña del piloto, trabada sin salida: el guard de `orders.js:405` bloquea el cobro de pagos Yape/Plin sin confirmar, pero la tarjeta del modal no tenía comprobante ni botón de confirmar — y esos pedidos son de días anteriores, así que ya no están en la Cola donde sí existen. El backend ya devolvía los datos; se agregaron `badgePago` + `comprobanteThumb` y una `confirmarPagoCierre()` propia (la de la Cola repinta `_cache`, no `_sinCerrar`). |
| [ISS-033](ISS-033-rutas-sin-autorizacion.md) | 14 rutas del panel con `authenticate` pero sin `authorizePermiso()` | 2026-08-12 | Salió de una pregunta sobre si el mozo podía reservar desde su panel (no puede: esa pantalla no existe). La autenticación nunca faltó — faltaba la autorización. Con el rol `pensionista` nuevo, un comensal logueado podía listar todas las reservas con nombre y teléfono de los clientes, y saltarse su propio descuento de saldo llamando a `POST /api/orders`. Además ambos POST tomaban `id_restaurante` del body en vez del token (creación en restaurante ajeno). En una segunda tanda se cerraron también los 7 GET de catálogo de `menu.js`/`mesas.js`, tras confirmar que la carta pública usa `/api/public/*` y no esos endpoints. 29 tests nuevos. |
| [ISS-032](ISS-032-rate-limit-login-correctos.md) | Rate limiter de login contaba también intentos correctos — riesgo de bloquear personal el día de la primera atención masiva | 2026-08-11 | Encontrado en una prueba de carga manual (60 pedidos simultáneos: 0 errores; 10 logins simultáneos con contraseña correcta: 1 bloqueado con 429). Faltaba `skipSuccessfulRequests: true` en `loginLimiter`. 4 tests nuevos. |
| [ISS-025](ISS-025-push-no-llega.md) | Push no llegaba pese a permisos + PWA instalada | 2026-08-11 | Causa raíz encontrada por SSH: VAPID keys del servidor regeneradas después de crearse las suscripciones — FCM las rechazaba con 403 para siempre. Fix: `owner.html` se autorrepara (da de baja + resuscribe si el navegador rechaza la clave actual); `pushNotificaciones.js` limpia también en 403, no solo en 410. 8 tests nuevos. |
| [ISS-031](ISS-031-badge-push-gris.md) | Ícono de notificación push aparece como cuadrado gris en vez del logo | 2026-08-11 | Android fuerza el `badge` (ícono chico de barra de estado) a una silueta monocromática — necesita fondo transparente. Se usaba `icon-192.png` (opaco) también como badge. Nuevo `badge-96.png` (monograma "MP" blanco sobre transparente) + `sw.js` bump a `v7` + los 4 puntos que disparan push actualizados. |
| [ISS-030](ISS-030-cocina-sin-filtro-fecha.md) | Cocina sin filtro por día (mismo patrón que ISS-026) + intervalos de polling muy cortos | 2026-08-11 | `GET /api/orders/activas` + `/api/reservations?flag=es_en_cocina` no filtraban fecha (con N+1). Nueva `cocinaDelDia()` en `utils/colaDia.js` (reutiliza `ordenesActivas`/`reservasActivas` de ISS-026) + `GET /api/orders/cola-cocina`. Intervalos subidos: Cocina 15s→30s, Órdenes/Reservas/Mesas 10s→20s. |
| [ISS-029](ISS-029-cargo-tapper-fijo.md) | Cargo de tapper fijo en vez de por unidad (2 menús para llevar solo sumaban 1 tapper) | 2026-08-11 | `cargo_modalidad` era un monto fijo por orden/reserva en vez de escalar por cantidad. Nueva `contarUnidadesMenu()` en `utils/menuPricing.js` (deduce cuántos menús se pidieron contando filas de secciones obligatorias por `id_menu_dia` — no hay ID de "instancia de menú" en la BD) + `calcularCargoModalidad()` en `routes/public.js`: `costo_tapper × (unidades de menú + unidades de carta)`, más tarifa de delivery fija si aplica. Ampliado a pedido del usuario para que la carta también sume tapper por ítem. |
| [ISS-028](ISS-028-overflow-letra-grande.md) | Scroll horizontal en el panel al agrandar la letra | 2026-08-10 | Encontrado al medir antes de subir la escala tipográfica, no reportado. Dos flex items sin `min-width:0` que nunca encogían: el bloque "Link del menú"/QR de Configuración (desbordaba ya a 1.15×, el nivel "Grande" activo en producción) y `.page-title` del topbar (desde 1.4×, afectando todos los paneles). Fix: `min-width:0` + `text-overflow:ellipsis` + padding del topbar en px. Tabs, carrusel de Home y tabla de Usuarios resultaron falsos positivos — ya tenían su propio `overflow-x:auto`. |
| [ISS-027](ISS-027-sesion-se-pierde.md) | Hay que iniciar sesión cada vez que se abre la app | 2026-08-10 | Dos causas: JWT/cookie de 8h **y** sesión guardada en `sessionStorage` (se borra al cerrar la PWA) — esta última era la real. Sesión de 30 días con renovación deslizante vía `GET /api/auth/me` (admin acotado a 1 día), sesión movida a `localStorage` con migración automática, `sameSite: 'lax'`, splash antes del paint en `login.html` y limpieza de sesión en el 401 para evitar el bucle login ↔ panel. |
| [ISS-026](ISS-026-cola-carrera-doble-tap.md) | Cola del día: los pedidos no cambian de etapa, vuelven atrás o dan error falso | 2026-08-10 | Carrera entre el poll y los taps: sin guard de botón (doble tap → error falso "No se puede cambiar una orden pagado"), sin token de secuencia (respuestas de polls viejos repintaban el estado anterior) y sin feedback inmediato. Fix: guard por ítem, token de secuencia, actualización optimista con reversión, y nuevo `GET /api/orders/cola` que reemplaza 6 requests con N+1 por 1 llamada con consultas fijas (`utils/colaDia.js`). Se agregó el **cierre de caja** para los pedidos de días anteriores, que hasta ahora quedaban con `total NULL` e invisibles en Ganancias. |
| [ISS-024](ISS-024-uploads-sin-cache-control.md) | Fotos de `/uploads` se re-descargan en cada carga (sin caché HTTP) | 2026-07-14 | `express.static` sin `max-age`. Como los nombres de archivo ya son versionados (timestamp único por subida), se agregó `Cache-Control: public, max-age=31536000, immutable` solo para `/uploads`. |
| [ISS-023](ISS-023-cola-lenta-reservas-sin-filtrar.md) | Cola del día se pone lenta en horas pico (`GET /api/reservations` traía todo el historial) | 2026-07-14 | `pedidos.js` pedía reservas sin filtro (+ N+1) cada 15s de polling; bloqueaba el proceso Node entero (síncrono). Cambiado a 5 llamadas por `flag`, igual que `reservas.js`. Auditado con 3000 reservas sembradas: 540ms→38ms, 14.2× más rápido. |
| [ISS-022](ISS-022-service-worker-cache-desactualizado.md) | Service Worker sirve `owner.html`/`owner.css` viejos aunque el deploy sea correcto | 2026-07-14 | `CACHE` en `sw.js` no se bumpeaba desde 2026-05-29 — el navegador nunca refrescaba el caché de `owner.html`/`css/owner.css`. Bumpeado a `menupro-v3`. Probablemente explica por qué varios fixes previos "no se veían". |
| [ISS-021](ISS-021-comprobante-rompe-pwa.md) | Foto de comprobante en Cola no carga / rompe la app instalada | 2026-07-13 | `<a target="_blank">` sobre una PWA standalone instalada rompe el contenedor de la app (sobre todo en iOS). Reemplazado por modal in-app (`comprobanteThumb()` unificado en `utils.js`). |
| [ISS-020](ISS-020-error-handler-sin-contexto.md) | Error handler global no loguea ruta ni stack trace | 2026-07-13 | `app.js` solo logueaba `err.message`. Ahora loguea `req.method`, `req.originalUrl` y `err.stack`. |
| [ISS-019](ISS-019-trust-proxy.md) | `trust proxy` no configurado — rate-limiter falla en casi cada request | 2026-07-13 | Servidor detrás de Nginx sin `app.set('trust proxy', 1)`. Agregado en `app.js`. |
| [ISS-018](ISS-018-boton-pago-sin-scroll.md) | Botón "Ya pagué" no visible / sin scroll en pantalla de pago | 2026-07-13 | `#pago-screen` sin `overflow-y`. Agregado `overflow-y:auto` + `justify-content:flex-start` (mismo patrón que `#estado-screen`). |
| [ISS-017](ISS-017-boton-abrir-yape-roto.md) | Botón "Abrir Yape" abre página inexistente en `menu.html` | 2026-07-13 | `https://yape.com.pe/cobrar?phone=XXXX` no es un endpoint real de Yape. Reemplazado por botón "Copiar número" igual que Plin, sin deep link inexistente. |
| [ISS-016](ISS-016-toggle-elegible-visible-no-actualiza.md) | Toggles "Cliente elige / Fijo" y "Visible / Oculto" no actualizan la UI | 2026-06-15 | `toggleElegibleMenu` y `toggleActivoMenu` solo llamaban `loadMenusDia()` (recarga la galería oculta). Fix: agregar `recargarModalConfig()` en ambas funciones para re-renderizar la vista de config activa. |
| [ISS-015](ISS-015-foto-plato-menu-error.md) | La foto de un plato no se actualiza en pantalla tras "Cambiar foto" (caché por nombre de archivo fijo) | 2026-06-06 | Nombre versionado `plato_<id>_<Date.now()>.<ext>` en `makeUploadPlato`. URL nueva por subida rompe caché del navegador; el borrado del anterior nunca pisa la imagen recién subida. Desplegado a producción 2026-06-15. |
| [ISS-014](ISS-014-reporteria-owner.md) | Revenue Total y Ganancia de hoy siempre S/0.00 en reportería | 2026-06-03 | Bug 1: `GET /api/orders` no devuelve `es_pagado` → filtro siempre vacío → revenue = 0. Fix: usar `resumen.total` de `/api/reportes/ganancias/resumen`. Bug 2: `date('now')` en SQLite usa UTC, fechas en BD usan Lima (UTC-5) → ganancia de hoy = 0 después de 19h. Fix: `date('now', '-5 hours')` en resumen/mes/semana. |
| [ISS-013](ISS-013-sw-bloquea-cdn.md) | Service worker rompe CDN/fuentes cross-origin (landing sin estilos) | 2026-05-29 | `sw.js` interceptaba peticiones cross-origin y las reenviaba con `fetch(e.request)` → `ERR_FAILED` en Tailwind CDN / Google Fonts. Fix: `if (url.origin !== self.location.origin) return;` para no tocar cross-origin + bump cache `v1`→`v2`. Afectaba landing y potencialmente owner/menu para usuarios recurrentes. |
| [ISS-010](ISS-010-orden-cocina.md) | Orden incorrecto en panel Cocina — Pendientes antes que En preparación | 2026-05-23 | Render reordenado en `cocina.js`: En preparación → Reservas en prep → Pendientes |
| [ISS-009](ISS-009-cookies-agresivos.md) | Panel "Cola del día" queda en "Cargando…" con JWT expirado | 2026-05-23 | `api()` en `utils.js` detecta 401 y redirige a `/login.html` automáticamente |
| [ISS-008](ISS-008-reserva-no-pasa-a-cocina.md) | Reserva no aparece en cola de cocina al pasar a "En preparación" | 2026-05-23 | `cocina.js` fetcha `GET /api/reservations?flag=es_en_cocina` en paralelo; nueva sección + `renderCocinaReserva()` + `marcarReservaListaCocina()` |
| [ISS-007](ISS-007-kitchen-no-eliminado.md) | kitchen.html no eliminado + cocinero a página en blanco + falta permiso Cocina | 2026-05-23 | login.html redirect corregido; kitchen.html → redirect; permiso `cocina` en PERMISOS_DEF; guard extendido para rol cocinero |
| [ISS-006](ISS-006-eleccion-+status-reserva.md) | Solo dos estatus en reservas — flujo incompleto | 2026-05-23 | Agregados flags intermedios al SELECT; `loadReservasActivas` fetcha 5 estados; botones completos en tarjetas |
| ISS-012 | Usuarios con permiso de reservas/órdenes reciben 403 al cambiar estatus | 2026-05-25 | `authorize('owner','mozo')` reemplazado por `authorizePermiso()` en 7 endpoints de `reservations.js` y `orders.js`. Afectaba: PATCH estatus, PATCH mesa, PATCH confirmar-pago (reservas); PATCH estatus, PATCH confirmar-pago, GET queue, PUT kitchen (órdenes). |
| [ISS-002](ISS-002-pago-plin.md) | Botón "Ya pagué con Plin/Yape" deshabilitado en segunda transacción | 2026-05-27 | `showPagoStep()` ocultaba el botón pero no reseteaba `disabled`. En una segunda orden de la misma sesión, el botón aparecía con `disabled=true`. Fix: `btnPague.disabled = false` en `showPagoStep()` + en cada rama de `seleccionarMetodoPago()`. |
| [ISS-012-admin](ISS-012-admin.md) | Admin: Revenue S/0.00 en tabla + gráficas Demanda/Ganancias vacías | 2026-05-26 | Chart.js faltante en dashboard.html; revenue calculaba solo carta omitiendo menú del día y reservas. |
| [ISS-011](ISS-011-security-policy.md) | Console Issues: CSP eval() + 27 "No label" en form fields | 2026-05-25 | 27 inputs/selects con `<label>` sin `for` corregidos en `owner.html` y `menu.html` (añadido `for="id"` en todos). Inputs sin label: `aria-label` añadido. eval() es de QRCode.js CDN — documentado en `deploy.md` sección 8.2 (CSP Helmet con `'unsafe-eval'` para esa CDN). |
| [ISS-005](ISS-005-uploads-missing-dirs.md) | ENOENT al subir foto de plato en laptop nueva | 2026-05-22 | `mkdirSync` en arranque de `routes/menu.js` para las 3 subcarpetas de uploads |
| [ISS-004](ISS-004-bom-encoding.md) | Caracteres corruptos en owner.html tras edición con PowerShell | 2026-05-22 | PowerShell 5.1 `Set-Content -Encoding utf8` agrega BOM — re-guardado con `[System.Text.UTF8Encoding]::new($false)` |
| [ISS-001](ISS-001-pago-plin.md) | Confirmar pago con Plin falla a veces (owner.html) | 2026-05-21 | Eliminado el doble mecanismo: botón "Confirmar pago" removido de tarjetas; al pasar a `es_full`/`es_pagado` el backend setea `estado_pago='pagado'` automáticamente (REFACTOR-001) |
| [ISS-003](ISS-003-estatus-flag-500.md) | PATCH estatus con flag retorna 500 (órdenes y reservas) | 2026-05-21 | Eliminado `AND id_restaurante IS NULL` inválido de 3 queries en `orders.js` y `reservations.js` |

---

## Refactors pendientes

| ID | Título | Estado |
|----|--------|--------|

## Refactors cerrados

| ID | Título | Fecha cierre |
|----|--------|--------------|
| [REFACTOR-001](REFACTOR-001-estatus-dinamicos.md) | Estatus dinámicos por flags (bloqueante para pagos) | 2026-05-21 ✅ |
