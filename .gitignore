# Dependencies
node_modules/

# Environment (secretos — NO subir)
.env

# Database (se genera al iniciar)
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Uploads generados por usuarios (fotos de platos, comprobantes de pago).
# NO versionar: son datos de producción y chocan con git pull en el deploy (ISS-005).
# Las carpetas se recrean solas al arrancar (routes/menu.js, routes/public.js).
public/uploads/

# Logs
*.log

# OS / IDE
.DS_Store
.vscode/
.idea/
Thumbs.db

